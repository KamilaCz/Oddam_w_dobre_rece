import React from "react";

export default function Button ({children}) {
    return (
        <>
        <button className="main__button" >
            {children}
            </button>
            </>
    )
}
